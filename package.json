{
  "name": "reqsmith",
  "version": "0.1.0",
  "private": true,
  "description": "A code-aware API development environment",
  "scripts": {
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all",
    "lint": "nx run-many --target=lint --all",
    "dev": "nx serve desktop",
    "format": "nx format:write"
  },
  "devDependencies": {
    "@nx/workspace": "^21.3.0",
    "@nx/js": "^21.3.0",
    "@monodon/rust": "^2.0.0",
    "nx": "^21.3.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "workspaces": [
    "apps/*",
    "libs/*"
  ]
}
