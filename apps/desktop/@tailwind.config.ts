const config = {
  darkMode: ['selector', '[data-theme="dark"]'], // Enable dark mode with selector and data attribute
  theme: {
    extend: {
      // Add any custom theme extensions here
    },
  },
};

export default config;
