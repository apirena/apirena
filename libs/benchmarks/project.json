{
  "name": "benchmarks",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/benchmarks/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@monodon/rust:build",
      "options": {}
    },
    "test": {
      "executor": "@monodon/rust:test",
      "options": {}
    },
    "run:bench_demo": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo run --package pinpath-benchmarks --bin bench_demo"
      }
    },
    "bench:parser_micro": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo bench --package pinpath-benchmarks --bench parser_micro"
      }
    },
    "bench:watcher_micro": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo bench --package pinpath-benchmarks --bench watcher_micro"
      }
    },
    "bench:end_to_end": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo bench --package pinpath-benchmarks --bench end_to_end"
      }
    },
    "bench:scalability": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo bench --package pinpath-benchmarks --bench scalability"
      }
    },
    "run:bench_demo:dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo run --package pinpath-benchmarks --bin bench_demo"
      }
    }
  },
  "tags": ["type:library", "scope:pinpath"]
}
